
devemos utilizar o array fruits;
let fruits = [3, 4, 10, 1, 12];
o termo "percorra" indica uma estrutura de repetição, ou loop;
"somando todos os valores" mostra que devemos ter uma variável que guarda o valor da soma dos valores;
o termo "caso" indica uma estrutura condicional, ou if;
"imprima o valor final" indica um console.log;


Adicionar o array;
let fruits = [3, 4, 10, 1, 12];

Criar uma variável com valor 0;
let fruits = [3, 4, 10, 1, 12];
let sum = 0;

Criar um loop que percorre o array;
let fruits = [3, 4, 10, 1, 12];
let sum = 0;

for (let index = 0; index < fruits.length; index += 1) {

}

Incrementar a variável com o valor correspondente a cada loop;
let fruits = [3, 4, 10, 1, 12];
let sum = 0;

for (let index = 0; index < fruits.length; index += 1) {
  sum += fruits[index];
}

Criar um if com a condição da variável ser maior que 15;
let fruits = [3, 4, 10, 1, 12];
let sum = 0;

for (let index = 0; index < fruits.length; index += 1) {
  sum += fruits[index];
}

if (sum > 15) {

} else {

}

Caso não obedeça a condição;


let fruits = [3, 4, 10, 1, 12];
let sum = 0;

for (let index = 0; index < fruits.length; index += 1) {
  sum += fruits[index];
}

if (sum > 15) {
  console.log(sum);
} else {
  console.log('valor menor que 16');
}



5 - Utilizando for, descubra qual o maior valor contido no array e imprima-o;


- Precisamos do for
- verificação da array
- varial contendo o maior valor
- array
- imprimir


-Adicionar array
-Criar varial com 0 para armazenamento
-Criar um loop que verifica array
-Condição para verificar os valores e armazenar
-Fazer impressao do valor


6 - Descubra quantos valores ímpares existem no array e imprima o resultado. Caso não exista nenhum, imprima a mensagem: "nenhum valor ímpar encontrado";

-Criar array
-criar variavel de armazenamento
-Verificar os numeros impares
-condição caso for impar
-armazenar na variavel
-caso a varivel obdecer a condição
-guardar a vez que ela aparece na variavel criada
-caso variavel nao obdcer condição retornar o erro
-retornar o valor ou erro


7 - Utilizando for, descubra qual o menor valor contido no array e imprima-o;

criar array
criar variavel de armazenamento
criar for
Criar um loop que percorre o array e verficia o menor
armazenar menor numero na variavel criada
mostrar valor


8- Utilizando for, crie um array que vá de 1 até 25 e imprima o resultado;

Criar array com o valor inicial 1 
criar laço de  repetição
adicionar incremento ao valor inicial
incrementar até que seja menor que 26
exibir array


9 - Utilizando o array criado no exercício anterior imprima o resultado da divisão de cada um dos elementos por 2.
Criar array com o valor inicial 1 
criar laço de  repetição
Incrementar a variável com o valor correspondente a cada loop e dividir por 2
incrementar até que seja menor que 26
exibir array


*/


